{
  "name": "browser-control",
  "private": true,
  "packageManager": "pnpm@10.14.0",
  "scripts": {
    "build": "pnpm -r build",
    "check": "pnpm -r check",
    "fixtures": "node tests/fixtures/server.mjs",
    "test": "node --test tests/protocol.test.mjs && pnpm -r test",
    "format:check": "pnpm -r format:check"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "typescript": "^5.9.3"
  }
}
